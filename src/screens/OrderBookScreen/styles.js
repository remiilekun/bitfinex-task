import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  title: {
    color: "white",
    fontSize: 24,
    marginBottom: 10,
  },
});

export default styles;
